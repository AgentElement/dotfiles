[user]
    name = agentelement
    email = agentelement@protonmail.com
    signingkey = CD139E3B435434F1

[difftool]
    prompt = false

[init]
    defaultBranch = master

[pager]
    diff = delta
    log = delta
    reflog = delta
    show = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    features = line-numbers side-by-side

[commit]
    gpgsign = true

[tag]
    gpgsign = true

[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true

[rerere]
    enable = true

[lfs]
    cachecredentials = true
