[user]
    name = agentelement
    email = agentelement@agentelement.net
    signingkey = CD139E3B435434F1

[difftool]
    prompt = false

[init]
    defaultBranch = master

[pager]
    diff = delta
    log = delta
    reflog = delta
    show = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    features = line-numbers side-by-side

[diff]
    renames = true
    mnemonicPrefix = true
    colorMoved = plain
    algorithm = histogram

[fetch]
    all = true

[help]
    autocorrect = prompt

[commit]
    gpgsign = true
    verbose = true

[column]
    ui = auto

[branch]
    sort = -committerdate

[tag]
    gpgsign = true

[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true

[rerere]
    enable = true
    autoupdate = true

[lfs]
    cachecredentials = true
